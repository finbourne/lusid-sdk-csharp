# Lusid.Sdk.Model.OverrideVirtualTransactionsRequest
Upserts overrides and suppressions of virtual transactions generated by a single instrument event.  Overrides are keyed by the virtual transaction id being overridden; suppressions are the list of virtual  transaction ids being suppressed. A virtual transaction id must not appear in both. This is a full  replace of the previously stored overrides and suppressions for the targeted instrument event - any  previously stored entry not present in this request is removed.

## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------
**Overrides** | **Dictionary&lt;string, List&lt;OverrideDefinitionRequest&gt;&gt;** | A map of virtual transaction id to one or more replacement transactions. A virtual transaction id cannot also appear in Suppressions. | [optional] 
**Suppressions** | **List&lt;string&gt;** | The virtual transaction ids to suppress. A virtual transaction id cannot also appear in Overrides. | [optional] 

```csharp
using Lusid.Sdk.Model;
using System;

Dictionary<string, List<OverrideDefinitionRequest>> overrides = new Dictionary<string, List<OverrideDefinitionRequest>>();
List<string> suppressions = new List<string>();

OverrideVirtualTransactionsRequest overrideVirtualTransactionsRequestInstance = new OverrideVirtualTransactionsRequest(
    overrides: overrides,
    suppressions: suppressions);
```

[Back to Model list](../README.md#documentation-for-models) &#8226; [Back to API list](../README.md#documentation-for-api-endpoints) &#8226; [Back to README](../README.md)
